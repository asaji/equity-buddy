gemini_api_key: ""

pushover:
  user_key: ""
  api_token: ""

# Optional: base URL of the web UI used for deep links in Pushover notifications
# Example: http://192.168.1.100:7003
base_url: ""

schedule:
  scrape_interval_hours: 2
  digest_time: "07:00"
  timezone: "America/Los_Angeles"

conviction_alert_threshold: "high"

watchlist_alerts:
  gain_thresholds: [20, 50, 100]
  loss_threshold: -20

accounts:
  twitter: []
  substack: []
